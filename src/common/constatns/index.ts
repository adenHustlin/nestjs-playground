export enum SpaceRoleSet {
  ADMIN = 'admin',
  PARTICIPANT = 'participant',
  CREATOR = 'creator',
}

export enum PostType {
  NOTICE = 'notice',
  QUESTION = 'question',
}

export enum PostStatus {
  NEW = 'new',
  UPDATED = 'updated',
  NEWCHAT = 'new chat',
}
